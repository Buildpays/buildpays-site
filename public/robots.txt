# BuildPays — https://www.buildpays.com.au/
#
# Deliberately minimal. Cloudflare's managed robots.txt (AI Crawl Control, enabled on the
# buildpays.com.au zone) is PREPENDED to this file at the edge, and it already supplies:
#   User-agent: *  /  Content-Signal: search=yes,ai-train=no,use=reference  /  Allow: /
#   plus Disallow rules for Amazonbot, Applebot-Extended, Bytespider, CCBot, ClaudeBot,
#   CloudflareBrowserRenderingCrawler, Google-Extended, GPTBot and meta-externalagent.
#
# Repeating those here just duplicates every block in the served file, so this file carries
# only what Cloudflare does not: the sitemap location.
#
# Two things to know:
#   - Check the served output (curl https://www.buildpays.com.au/robots.txt) after changing
#     this, not just the repo copy — what ships is the merge of both.
#   - /buildpays-worker-training is intentionally NOT disallowed. It carries a noindex meta
#     tag, and a crawler must be able to fetch the page to see that tag.

Sitemap: https://www.buildpays.com.au/sitemap.xml
