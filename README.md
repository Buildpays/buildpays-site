# buildpays-site
Marketing site for BuildPays (buildpays.com.au). Static HTML — no build step.

- `index.html` — marketing site
- `buildpays-explainer.html` — animated "one week on site" (embedded via iframe on the homepage)
- `buildpays-worker-training.html` — worker training video (link from onboarding SMS)
- `_headers` — security headers (Cloudflare Pages)
- `_redirects` — bare domain → www

## Deploy (Cloudflare Pages)
Framework preset: **None** · Build command: *(blank)* · Output directory: `/`
Pushing to `main` deploys automatically.
